@inject IStringLocalizer<SharedResource> Loc

<body class="z-20 flex h-screen opacity-95">
    <div class="bg-gray-800 text-white fixed inset-y-0 left-0 z-40 flex w-64 flex-shrink-0 flex-col space-y-2 px-4 py-6 shadow-lg transition-transform duration-300 ease-in-out">
        <div class="mb-6 px-2">
            <a href="@RouteConfig.PLAYGROUND" class="hover:cursor-pointer"
                <h3 class="text-gray-200 text-xl font-semibold"> Playground</h3>
            </a>
        </div>
        <ResumeApp.Pages.Components.Localizer />

        @* Navigation Links *@
        @foreach (Route route in AppRouteConfig.NavigationMenuRoutes.Routes)
        {
            if (route.IsPublished)
            {
                <CustomNavLink MaterialIcon="@route.MaterialIcon"
                               RedirectionPath="@route.SubPath"
                               LocalizationPath="@Loc[@route.LocalizationNamePath]" />
            }
        }
        
    </div>
</body>
